// Identifier cannot start with a digit
config {
  base_url = "http://localhost:8080";
}

let 2user = "admin";

test TestIdentifier {
  GET "/api/test";
  expect status = 200;
  expect body contains "ok";
}